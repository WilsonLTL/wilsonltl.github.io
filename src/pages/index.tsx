// NextJS
import type { NextPage } from "next";
import { LoadingCompontent } from "../components/globalComponent";

const index = () => {
  return (
    <LoadingCompontent />
  );
};

export default index;
